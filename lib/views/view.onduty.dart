import 'package:flutter/material.dart';

class OndutyView extends StatelessWidget {
  const OndutyView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Onduty Page',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
